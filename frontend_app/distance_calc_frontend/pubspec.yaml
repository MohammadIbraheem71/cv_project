name: distance_calc_frontend
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.11.0
  google_mlkit_object_detection: ^0.13.0
  image: ^4.1.7
  shared_preferences: ^2.2.3
  tflite_flutter: ^0.12.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/models/midas_small.tflite
